Flask>=3.1,<4        # includes Werkzeug, Jinja2, click, itsdangerous, markupsafe
gunicorn>=22         # production web server (Linux/macOS; on Windows use `flask run` for local testing)
# psycopg[binary]>=3.2   # add when DATABASE_URL points to PostgreSQL
